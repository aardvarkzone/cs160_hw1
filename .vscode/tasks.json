{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "g++",
            "args": [
                "-g",                         // For generating debug information
                "${workspaceFolder}/calc.cpp", // Path to your source file
                "-o",
                "${workspaceFolder}/calc"      // Output binary
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
